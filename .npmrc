# Refuse to install on a Node.js that is too old (see "engines" in package.json). Without this, a server that
# defaults to an old Node builds fine and then crashes at start-up with confusing errors.
engine-strict=true
